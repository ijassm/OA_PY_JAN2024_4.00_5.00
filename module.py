import map_filter_reduce
import list
import math

arr = list.l

print(map_filter_reduce.myReduce(lambda acc, cv: acc + cv, arr, 10))

print(math.pi)
print(math.pow(2, 4))
print(math.factorial(5))
print(2**4)
